# Project pnpm settings live in pnpm-workspace.yaml (pnpm 11 reads install
# behaviour, hoisting, engine-strict, and build approvals from there, not from
# kebab-case keys here). This file is kept for registry/auth overrides only.
#
# Example (uncomment + edit for a private DHIS2 registry mirror):
# registry=https://registry.npmjs.org/
